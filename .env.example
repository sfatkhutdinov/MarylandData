# MarylandData Project Environment Variables
# Copy this file to .env and add your actual API keys

# =============================================================================
# CENSUS BUREAU API KEY (REQUIRED)
# =============================================================================
# Get your free API key at: https://api.census.gov/data/key_signup.html
# This is REQUIRED for demographic data collection from ZIP 21076
CENSUS_API_KEY=your_census_api_key_here

# =============================================================================
# BUREAU OF LABOR STATISTICS API KEY (OPTIONAL)
# =============================================================================
# Register at: https://data.bls.gov/registrationEngine/
# Required for detailed employment statistics (enhanced rate limits)
# Basic queries work without a key but have lower rate limits
BLS_API_KEY=your_bls_api_key_here

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Set to 'development' for verbose logging, 'production' for minimal logging
ENVIRONMENT=development

# API request timeout in seconds
API_TIMEOUT=30

# Rate limiting delay between requests (seconds)
RATE_LIMIT_DELAY=0.5

# =============================================================================
# DATA COLLECTION SETTINGS
# =============================================================================
# Default year for ACS data collection (use most recent 5-year estimates)
DEFAULT_ACS_YEAR=2023

# Geographic focus for all data collection
FOCUS_ZIP_CODE=21076
FOCUS_STATE_FIPS=24
FOCUS_COUNTIES=howard:027,anne_arundel:003

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Never commit the .env file to version control
# - API keys are free but should be kept secure
# - Census API key allows 500 requests per day per IP
# - BLS API key allows 500 requests per day (vs 25 without key)
# - Rotate keys if accidentally exposed